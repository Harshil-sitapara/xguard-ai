# ── Core numerical stack ──────────────────────────────────────────────────
# numpy 1.26.4: last 1.x; compatible with TF 2.19, XGBoost 2.1
numpy==1.26.4
pandas==2.2.3

# ── ML / Training ─────────────────────────────────────────────────────────
# scikit-learn 1.6.1: latest stable; requires imbalanced-learn>=0.13
scikit-learn==1.6.1

# imbalanced-learn 0.13.0: supports sklearn 1.6 (0.12.x does NOT)
imbalanced-learn==0.13.0

# xgboost 2.1.3: numpy 2.0-safe, fastest CPU hist training
xgboost==2.1.3

# ── Deep Learning (LSTM comparison model) ─────────────────────────────────
# TF 2.19.0: latest stable on Python 3.11 Windows (CPU-only on native Windows)
# GPU: use WSL2 — TF 2.11+ dropped native Windows GPU support
tensorflow==2.19.0

# ── Serialisation ─────────────────────────────────────────────────────────
joblib==1.4.2
pyarrow==19.0.1

# ── Visualisation ─────────────────────────────────────────────────────────
matplotlib==3.9.4
seaborn==0.13.2
